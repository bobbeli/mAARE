# pos install -> does install all listed PodFiles
platform :ios, '13.0'

target 'mAARE' do
  use_frameworks!

  # Pods for Flash Chat iOS13
  pod 'Starscream', '~>4.0.0'
  pod 'Alamofire', '~> 5.1'
  pod 'RxCocoa', '~> 5'
end
